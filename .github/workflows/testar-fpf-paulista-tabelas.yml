name: Testar FPF Paulista Tabelas (diagnóstico, manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  testar:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Baixar repositório
        uses: actions/checkout@v5

      - name: Instalar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Testar scraper FPF Paulista Tabelas (com debug-html)
        run: |
          python scrap_fpf_paulista_tabelas.py --once --ano 2026 --incluir-passados --debug-html

      - name: Salvar resultados do diagnóstico (NÃO mistura com dados de produção)
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/debug_fpf_paulista_tabelas 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "Sem mudanças para commitar"
          else
            git commit -m "Diagnóstico FPF Paulista Tabelas (texto bruto extraído de cada PDF)"
            git pull --rebase origin main || git rebase --abort || true
            git push
          fi
